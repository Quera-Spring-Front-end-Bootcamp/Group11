import ProjectSlice, { ProjectSliceTypes } from './ProjectSlice';
import CalenderViewSlice, { CalenderViewSliceTypes } from './CalenderViewSlice';
import BoardHeaderSlice, { BoardHeaderSliceTypes } from './‌BoardHeaderSlice';

export { ProjectSlice, CalenderViewSlice, BoardHeaderSlice };

export type {
  ProjectSliceTypes,
  CalenderViewSliceTypes,
  BoardHeaderSliceTypes,
};
